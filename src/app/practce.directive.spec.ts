import { PractceDirective } from './practce.directive';

describe('PractceDirective', () => {
  it('should create an instance', () => {
    const directive = new PractceDirective();
    expect(directive).toBeTruthy();
  });
});
